//Write a program that takes an integer input and prints whether it is even or odd.
let number=5
if(number%2==0)
{
    console.log(`Number ${number} is a even number. `);
}
else{
    console.log(`Number ${number} is a odd number. `);
}